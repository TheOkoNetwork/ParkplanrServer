"use strict";

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var UniversalPark = require("./index");

/**
 * Universal's Islands Of Adventure
 * @class
 * @extends UniversalPark
 */

var UniversalIslandsOfAdventure = function (_UniversalPark) {
        _inherits(UniversalIslandsOfAdventure, _UniversalPark);

        /**
         * Create a new UniversalIslandsOfAdventure object
         */
        function UniversalIslandsOfAdventure() {
                var options = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};

                _classCallCheck(this, UniversalIslandsOfAdventure);

                options.name = options.name || "Universal's Islands Of Adventure";
                options.timezone = options.timezone || "America/New_York";

                // set park's location as it's entrance
                options.latitude = options.latitude || 28.4722430;
                options.longitude = options.longitude || -81.4678556;

                // Univesral park ID
                options.park_id = options.park_id || "10000";

                // inherit from base class
                return _possibleConstructorReturn(this, (UniversalIslandsOfAdventure.__proto__ || Object.getPrototypeOf(UniversalIslandsOfAdventure)).call(this, options));
        }

        return UniversalIslandsOfAdventure;
}(UniversalPark);

module.exports = UniversalIslandsOfAdventure;
//# sourceMappingURL=universalislandsofadventure.js.map